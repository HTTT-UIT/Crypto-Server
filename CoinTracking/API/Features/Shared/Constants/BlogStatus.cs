namespace API.Features.Shared.Constants
{
    public enum BlogStatus
    {
        NEW = 0,
        ACCEPTED = 1,
        REJECTED = 2,
        WARNED = 3,
        LOCKED = 4,
        CLOSED = 5,
    }
}
